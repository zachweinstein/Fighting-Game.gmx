var follow = argument0;
var follow_spd = argument1;
var can_follow = argument2;

if (can_follow == true){
    direction = point_direction(x, y, follow.x, follow.y);
    speed = follow_spd;
}
//ssdsd sds
